---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - serviceaccount.yaml
  - rbac.yaml
  - installplan-approver-job.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: job-installplan-approver
    files:
      - installplan-approver.sh
