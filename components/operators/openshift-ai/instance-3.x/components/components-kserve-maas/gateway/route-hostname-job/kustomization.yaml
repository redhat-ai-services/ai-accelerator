apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - rbac.yaml
  - patch-route-host-job.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: job-patch-route-host
    namespace: openshift-ingress
    files:
      - patch-route-host.sh
